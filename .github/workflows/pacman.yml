name: Generate pacman animation (DISABLED)

# Workflow disabled: triggers commented out. Re-enable by restoring 'on' section
# on:
#   schedule:
#     - cron: '* */12 * * *' # Every 12 hours
#   workflow_dispatch:
#   push:
#     branches:
#       - main

jobs:
  generate:
    if: false # Hard disable job execution. Remove this line to re-enable.
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate pacman-contribution-graph.svg
        uses: abozanona/pacman-contribution-graph@main
        with:
          github_user_name: ${{ github.repository_owner }}

      - name: Push pacman-contribution-graph.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
